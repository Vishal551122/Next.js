/** @type {import('next').NextConfig} */
const nextConfig = {
      images:{
        domains: [`images.unsplash.com`, `randomuser.me`, `cdn.pixabay.com`,`media.istockphoto.com`],
      }


};

export default nextConfig;
